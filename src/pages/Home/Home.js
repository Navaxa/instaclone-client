import React from 'react';
import useAuth from '../../hooks/useAuth';
import './Home.scss';

const Home = () => {
    return (
        <div>
            <h1>Estamos en Home</h1>
        </div>
    );
};

export default Home;